# 图片资源目录结构

## 快速创建目录结构

图片资源统一放在 `miniprogram/images/` 目录下，建议按以下结构组织：

```
miniprogram/images/
│
├── tabbar/                 # 底部导航栏图标（必须）
│   ├── home.png
│   ├── home-active.png
│   ├── memory.png
│   ├── memory-active.png
│   ├── vr.png
│   ├── vr-active.png
│   ├── culture.png
│   └── culture-active.png
│
├── banner/                  # 轮播图（首页）
│   ├── banner1.jpg
│   ├── banner2.jpg
│   └── banner3.jpg
│
├── icons/                   # 功能图标
│   ├── icon-memory.png
│   ├── icon-vr.png
│   ├── icon-culture.png
│   ├── icon-local.png
│   ├── icon-ip.png
│   ├── icon-guide.png
│   ├── icon-vr-play.png
│   ├── icon-phone.png
│   └── icon-location.png
│
└── [其他图片可直接放在 images 根目录]
```

## 方案一：扁平化结构（推荐）

**所有图片直接放在 `miniprogram/images/` 根目录**，代码中引用 `/images/xxx.jpg`

```
miniprogram/images/
├── banner1.jpg
├── banner2.jpg
├── banner3.jpg
├── feature1.jpg
├── feature2.jpg
├── icon-memory.png
├── icon-vr.png
├── ... (其他图片)
└── tabbar/               # 仅 tabbar 图标单独分类
    ├── home.png
    ├── home-active.png
    └── ...
```

## 方案二：分类目录结构

**按功能分类组织图片**，需要在代码中更新路径

```
miniprogram/images/
├── tabbar/               # 底部导航栏
├── banner/               # 轮播图
├── icons/                # 功能图标
├── feature/              # 特色内容
├── history/              # 历史图片
├── heroes/               # 英雄头像
├── vr/                   # VR场景
├── products/             # 产品图片
├── landmarks/            # 地标图片
├── ip/                   # IP形象
├── scenes/               # 应用场景
├── pages/                # 页面背景
└── common/               # 通用图片
```

## 图片清单（共需 50+ 张）

### ⭐ 必需图片（优先准备）

#### 1. 底部导航栏图标（tabbar/）- **必须**
- `home.png` / `home-active.png` - 首页
- `memory.png` / `memory-active.png` - 红色记忆
- `vr.png` / `vr-active.png` - VR云游
- `culture.png` / `culture-active.png` - 文创
- **规格**: 81x81px, PNG格式

#### 2. 首页图片
- `banner1.jpg`, `banner2.jpg`, `banner3.jpg` - 轮播图
- `icon-memory.png`, `icon-vr.png`, `icon-culture.png`, `icon-local.png`, `icon-ip.png` - 功能图标
- `feature1.jpg`, `feature2.jpg` - 特色内容

### 📋 完整图片清单

详见 `图片资源清单.txt` 文件

## 快速开始

1. **创建目录**：
   - 在 `miniprogram` 下创建 `images` 文件夹
   - 在 `images` 下创建 `tabbar` 子文件夹

2. **准备图标**：
   - 优先准备 tabbar 图标（底部导航栏必需）
   - 建议使用设计软件制作或从图标库下载

3. **添加图片**：
   - 将图片按上述结构放入对应目录
   - 确保文件名与代码中引用一致

4. **测试**：
   - 在微信开发者工具中预览
   - 检查图片是否正确显示

## 注意事项

⚠️ **重要**：
- tabBar 图标路径已更新为 `images/tabbar/xxx.png`
- 其他图片路径保持 `/images/xxx.jpg` 格式
- 图片命名使用英文或拼音，避免中文
- 图片大小建议控制在合理范围（单张 < 500KB）
- 确保图片路径与代码引用路径一致

